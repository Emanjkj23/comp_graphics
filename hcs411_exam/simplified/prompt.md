I need you to create me a `cheat.cpp` file, that contains various shapes to draw, both 2D and 3D. Consider this to be  a "Cheat Sheet" file with functions for: drawTriangle(), drawCube(float size), drawCylinder(float radius, float height), setupBasicLighting(), drawCoordinateAxes() (This helps you see where $X, Y, Z$ are while debugging), and many more other functions. Let's include only the most frequesntly used functions, that could be used in an exam since our exam is going to be open book. Don't forget to add a `setupBasicCamera()` and a basic setup for lighting, shadowing and shading and also basic color and blending. 
NOTE: This is for opengl legacy, meaning no use of GLFW, GLAD, GLM, etc. Ensure every 3D shape function includes Normals (glNormal3f) so lighting works. For the setupBasicLighting, please include a 'Toggle' function to switch between Ambient and Specular so I can demonstrate the difference if asked. Also, include a drawGrid function that draws lines on the $X$ and $Z$ planes to represent a floor. In a high-pressure exam, scrolling through 500 lines of a single cheat.cpp is a nightmare. I recommend this "Exam Bundle" structure:
boiler.cpp: The skeleton (Main, Reshape, Basic Camera).
geometry.cpp: Your library of shapes (Cube, Teapot, Table, Chair).
effects.cpp: Lighting setup, material properties, and alpha blending.
input.cpp: Basic keyboard and mouse templates (e.g., "Press 'W' to move forward").
