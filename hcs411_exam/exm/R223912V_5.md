# QUESTION 5 (a-b)
```bash
// Basic camera setup
void setupBasicCamera() {
    glMatrixMode(GL_PROJECTION);
    glLoadIdentity();
    glFrustum(-5, 5, -5, 5, 15, 150);
    glMatrixMode(GL_MODELVIEW);
    glLoadIdentity();
    drawIguana();
    gluLookAt(0, 0, 15,    // eye
              0.0, 0.0, 0.0,  // center
              0.0, 1.0, 0.0);      // up
}
```
I Chaged line 6 to 15 for eye x

changed line 1 parameter to `GL_PROJECTION`

# QUESTION 5 (c)
```bash
// Basic camera setup
void setupBasicCamera() {
    glMatrixMode(GL_PROJECTION);
    glLoadIdentity();
    glFrustum(-5, 5, -5, 5, 15, 150);
    glMatrixMode(GL_MODELVIEW);
    glLoadIdentity();
    drawIguana();
    gluLookAt(0, 0, 15,    // eye
              0.0, 5.0, 0.0,  // center
              0.0, 1.0, 0.0);      // up
}
```

I Chaged line 6 to 5 for center middle

# QUESTION 5 (d)
```bash
// Basic camera setup
void setupBasicCamera() {
    glMatrixMode(GL_PROJECTION);
    glLoadIdentity();
    glFrustum(-5, 5, -5, 5, 15, 150);
    glMatrixMode(GL_MODELVIEW);
    glLoadIdentity();
    drawIguana();
    gluLookAt(0, 15, 10,    // eye
              0.0, 1.0, 0.0,  // center
              0.0, 1.0, 0.0);      // up
}
```
I Chaged line 6 to 5 for up middle
